---
"industrial-model": minor
---

Add optional `timeZone` on `calculate` / `calculateMultiples` so hour-and-longer CDF aggregates follow a local calendar, and fill a bucket grid when `rolling_average` runs over a uniform aggregate granularity.
